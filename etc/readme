These files are here for convenience. System versions will be loaded if available.

mime.types
==========

/etc/mime.types will be used if present.

This file is periodically replaced with the version at:

  http://svn.apache.org/repos/asf/httpd/httpd/trunk/docs/conf/mime.types
